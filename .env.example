# Markdown Notes App environment template
#
# Copy this file to `.env` and fill in the values for your environment.
# Do not commit the filled-in `.env` file to version control.

# Notes root directory for all markdown content.
# Can be an absolute path or a path relative to the app root.
# If omitted, the app defaults to a `notes/` folder under the app root.
# NOTES_ROOT=notes

# FastAPI / Uvicorn host and port used when running `python main.py` directly.
# HOST=127.0.0.1
# PORT=8000
# UVICORN_RELOAD=true

# GitHub fine-grained token for future GitHub API integrations.
# This is optional for basic notes syncing via Git alone.
# Never commit a real token to source control.
# GH_ACCESS_TOKEN=ghp_your_token_here

# Remote URLs for GitPython-based notes and app repositories.
# These are used by auto-commit/auto-pull/auto-push and manual sync actions.
# NOTES_REPO_REMOTE_URL="https://github.com/your-user/markdown-notes.git"
# APP_REPO_REMOTE_URL="https://github.com/your-user/markdown-notes-app.git"
